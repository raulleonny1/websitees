rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simulador público de la landing (solo demo, sin datos sensibles)
    match /landing_simulator/{docId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
